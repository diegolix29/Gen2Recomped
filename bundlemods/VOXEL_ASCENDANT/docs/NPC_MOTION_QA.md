# Current coverage: 198 / 198 exact arm sources (2026-09-11)

## Daisy on the Blue House stool — 2026-09-11

The exact pre-map Daisy object now uses the reviewed runtime seat transform on
its real stool and turns only its head during its owned conversation. The
post-map walking/standing Daisy is a separate index and does not acquire the
seat. Native Gen1, Classic, HD-off, story replacement, bounded resources and
the full runner pass. Source PNG unchanged. Evidence: `daisy-states/`.

Pokefan Female and both alpha-identical Gen2 palettes now use exact basket
guards. The basket hand and planted leg remain a coherent neutral unit while
the free arm and opposite leg move. Enlarged production output has no detached
basket, hand, or foot fragments. Native Gen1 and Gen2 runs cover base binding,
Gen1 palette rejection, Gen2 palette admission, Classic/HD-off/Natural return,
and exact pixel guards. The complete runner passes with all 198 catalogued arm
sources admitted. Blink coverage remains 84 sources. This closes source
registration; broad side-cadence, acting-pose, and performance acceptance are
still separate. Evidence: pokefan-basket/ in the review package. Historical
entries follow.

# Historical coverage: 195 / 198 exact arm sources (2026-09-11)

Mt. Silver Red now uses an exact runtime trim for the detached sole fragment
in its left-facing source row; the source PNG remains byte-identical. Capped
Youngster and both alpha-identical palettes use a separate exact Gen2 profile.
The existing Gen1 binder rejects all three capped files and continues with the
approved bald artwork. Enlarged galleries, exact pixel guards, native Gen1 and
Gen2 fallback checks, and the full runner pass with 195 admitted sources. Only
the three Pokefan Female basket sources remain pending. Blink coverage remains
84 sources. Evidence: red-mt-silver/ and youngster-cap/ in the review package.
Historical entries follow.

# Historical coverage: 191 / 198 exact arm sources (2026-09-11)

Agatha ist jetzt ohne PNG-Änderung freigegeben. Eine quellgebundene statische
Region hält den Stock aus den fehlerhaft verkürzten Schrittspalten heraus;
Füße und freie Körperbereiche verwenden weiter den normalen Rig-Pfad. Die
vergrößerte Zwölf-Karten-Ansicht, exakte Pixelwächter und native Gen1-/Gen2-
Läufe samt Classic/HD-aus/Natural-Rückkehr bestehen. Sechs Armquellen bleiben
offen. Evidence: agatha-static-cane/README.md. Historical entries follow.

# Historical coverage: 190 / 198 exact arm sources (2026-09-11)

31 weitere Alltags-, Arenaleiter- und Spezialrollen besitzen exakt gebundene
Armprofile. Dazu gehören unter anderem Misty Gen2, Erika, Janine, Sabrina,
beide Lance-Quellen und Lt. Surge Gen2. Stock, Fächer, Umhang und belegte
Armseiten bleiben je Quelle geschützt. Zehn wichtige Rollen erhielten zudem
neue Augenprofile; Sabrina blinzelt wegen der Haarüberdeckung nur frontal.

Native Gen1-/Gen2-Läufe, Rollen-Fallback, vergrößerte Sichtprüfung, voller
Runner und GPU-Körpervergleich mit 84 Quellen/996 Fällen/36.976.500 Pixel
bestehen. Alle 31 Quell-PNGs dieses Blocks sind gegenüber Commit `19039d6c`
bytegleich. Sieben Armquellen blieben in diesem Block gesperrt: Agathas Stock, der Korb der
Pokéfan-Frau samt Varianten, die defekte Rot-Silberberg-Quelle und zwei
Varianten des bereits runtime-seitig abgelehnten Youngsters. Die Sitzpose der
Pokéfan-Frau bleibt unabhängig davon freigegeben. Evidence:
near-complete-residents/README.md. Historical entries follow.

# Historical coverage: 159 / 198 exact arm sources (2026-09-11)

Juggler, Burglar, Cue Ball, Jr. Trainer Male, Cook, Silph Worker Female,
Middle-aged Woman und Safari Zone Worker besitzen jetzt exakt gebundene
Armprofile. Die beiden geprüften Farbvarianten der Middle-aged Woman sind in
Gen2 enthalten und bleiben in Gen1 entsprechend der bestehenden Binder-Regel
gesperrt. Handtasche, Sack und die jeweils belegte Körperseite sind in den
betroffenen Ansichten geschützt.

Sieben Rollen sowie die beiden Middle-aged-Woman-Varianten haben neue
Augenprofile. Der maskierte Burglar bleibt für prozedurales Blinzeln ausdrücklich
gesperrt. Native Gen1-/Gen2-Läufe, Rollen-Fallback, visueller Vergleich, voller
Runner und GPU-Körpervergleich mit 74 Quellen/884 Fällen/32.818.500 Pixel
bestehen. 38 Armquellen bleiben offen, eine ist runtime-seitig abgelehnt. Alle
zehn Quell-PNGs des Blocks sind gegenüber Commit `19039d6c` bytegleich. Die
Läufe verwenden synthetisch eingesetzte Testfiguren und sind keine vollständige
Storyfreigabe. Evidence: next-residents/README.md. Historical entries follow.

# Historical coverage: 149 / 198 exact arm sources (2026-09-11)

Sechs weitere häufig genutzte Bewohner besitzen exakt gebundene Arm- und
Augenprofile: Link Receptionist, Channeler, Psychic, Silph Worker Male,
Picnicker Gen2 und Little Girl. Native Gen1-/Gen2-Läufe, Rollen-Fallback,
voller Runner und GPU-Körpervergleich mit 65 Quellen/776 Fällen/28.809.000
Pixel bestehen. 48 Armquellen bleiben offen, eine ist runtime-seitig
abgelehnt. Quell-PNGs bleiben bytegleich. Die Läufe verwenden synthetisch
eingesetzte Testfiguren und sind keine Storyfreigabe. Seitliche Quellkadenz
bleibt offen. Evidence: high-usage-residents/README.md. Historical entries
follow.

## Neuborkia: Mutter und Besucherin sitzen — 2026-09-11

Die echten Objekte 1 und 5 in PLAYERS_HOUSE_1F erhalten im Natural-Modus
quellgenaue Sitzmeshes auf ihren tatsächlichen Stühlen. Beide Körper schauen
zum Tisch; die Besucherin behält ihren zusammenhängenden Korb. Im eigenen
Dialog wechselt nur der Kopf bei geschlossenem Auge zur Frontansicht, während
der Seitenkörper sitzen bleibt. Mutter blinzelt vorn und seitlich. Die
Besucherin blinzelt seitlich im Idle und verwendet im Gespräch ihr bereits
gezeichnetes lächelndes Frontgesicht mit geschlossenen Augen.

Zwei native Läufe mit den echten Dialogskripten, Classic, HD aus und Natural-
Rückkehr bestehen. Natural zeichnet pro Rolle ausschließlich die 165×225-
Sitzkarte, Classic ausschließlich den vollständigen 495×900-Atlas. Voller
Runner und GPU-Körpervergleich mit 50 Quellen/596 Fällen/22.126.500 Pixel
bestehen. Das Pokéfan-Augenprofil ist `seatedOnly`; das verworfene Korb-Armrig
bleibt per Negativtest gesperrt. Quellen bytegleich. Andere Stühle, versetzte
Storypositionen und allgemeine Seitenkadenz bleiben offen. Evidence:
johto-seats/README.md.

# Historical coverage: 116 / 198 exact arm sources (2026-09-11)

Rocket-Agentin, Gen2-Schwimmerin und Twins besitzen jetzt geprüfte Armprofile; die beiden Paletten von Schwimmerin und Twins sind exakt gebunden. Schwimmerin und Twins blinzeln außerdem auf sechs exakten Quellen. Ein fehlerhafter Rocket-Augenentwurf wurde entfernt und per Negativtest ausgeschlossen. Native Gen1-/Gen2-Läufe, voller Runner, GPU-Körpervergleich und die gegen alle Runtime-Pfade abgeglichene Coverage-Liste bestehen. 81 Quellen bleiben offen, eine ist runtime-seitig abgelehnt; 45 Augenquellen. Seitliche Quellkadenz und echte Storyszenen bleiben offen. Evidence: female-common/README.md. Historical entries follow.

# Historical coverage: 99 / 198 exact arm sources (2026-09-11)

Lorelei admitted after hand/bracelet and skirt-transition review, both native renderer/fallback tests and full runner. Agatha stays pending: the second-step source shortens or omits the cane, and the candidate was removed from production. 98 pending / 1 runtime-rejected. Blink count remains 29. Evidence: kanto-elite-rig/README.md. Historical entries follow.

# Historical coverage: 98 / 198 exact arm sources (2026-09-11)

Both exact Koga sources have reviewed hand regions and protected clothing. Gen1 front/rear alternation is corrected; Johto keeps its authored knee emblem and still needs gait artwork work. Native Gen1/Gen2 and full runner pass. 99 pending / 1 runtime-rejected. Blink source count 29. Evidence: koga-rig/README.md and koga-blink/README.md. Historical entries follow.

# Historical coverage: 96 / 198 exact arm sources (2026-09-11)

Karen and Will now have reviewed exact hand profiles and alternate front/rear steps. Karen rear hair stays static under arm swing; Will lower trousers/coat transition corrected. Both native generations, per-role fallback and full runner pass. 101 pending / 1 runtime-rejected. Side gait, acting and new blink coverage remain incomplete. Evidence: elite-four-rig/README.md. Historical entries follow.

# Historical coverage: 94 / 198 exact arm sources (2026-09-11)

Bruno and Chuck have reviewed hand profiles and front/rear alternate steps. The lower trouser transition was corrected after visual inspection. Both native generations, per-role fallback and the complete human-motion runner pass. 103 sources remain pending and one is runtime-rejected. Continuous side cadence is not fully accepted. Evidence: martial-leaders/README.md in the review package. Historical entries follow.

# Historical coverage: 92 / 198 exact arm sources (2026-09-11)

Archer and Ariana now have reviewed arm/glove profiles. Archer rear foot alternation and Ariana lower-coat/leg continuity were corrected after enlarged visual review. Per-role Classic/HD-off/Natural tests pass in both native renderer generations. 105 sources remain pending and one is runtime-rejected. Evidence and limits: rocket-executives/README.md in the review package. Historical entries follow.

# Historical coverage: 90 / 198 exact arm sources (2026-09-11)

Giovanni now has reviewed hand landmarks, protected suit hem and restrained idle shift. Both native renderer generations and the full human-motion runner pass. 107 sources remain pending and one is runtime-rejected. This is arm coverage, not complete animation/acting coverage. Detailed evidence: giovanni-rig/README.md in the review package. Historical entries follow.

# NPC arm profiles — first four sources

Natural card animation now admits the exact shipped Oak, Brock, Misty and Elm
walk atlases through human_rig_profiles.lua. `oak` aliases `professor-oak` for
the existing protagonist-preview route. Ordinary NPC binding uses the source
role. Other artwork, including unreviewed variants, is not silently matched.

Hand landmarks were reviewed in all four directions. Numeric skin-component
candidates are recorded in npc-hand-probe.json; Misty's side hands were placed
manually because her wristbands split the skin mask. Brock's hand anchor uses
the lower end of the connected forearm component. No raster artwork was edited.

Enlarged production-mesh galleries exposed ghosted hand pixels on the coat
sides: the neutral-upper/walking-lower blend crossed a hand. These four profiles
now protect their neutral arm/hand region during cell composition, before the
continuous mesh deformation. The existing six hero profiles keep their former
composites byte-for-byte. The protection is profile-specific; it does not alter
classic, Vanilla, action cards or unrecognized sources.

Native non-Orange checks:

- All four NPC records admitted separately from the player, in four directions,
  with independent presentation state, in both Gen1 and Gen2 fixtures.
- Dense-versus-reduced geometry comparison and actor-mesh isolation pass.
- All 48 NPC cells retain the neutral pixels in the protected core and the
  former composition outside the protection region (within one channel step).
  The transition fringe is visually reviewed rather than claimed identical.
- All 72 original hero cells remain byte-identical after the shader extension.
- Existing context, classic/Vanilla, dialog, movement and resource checks pass.
- Production-mesh neutral/A/B galleries for all four NPCs were visually viewed;
  the corrected coat hands remain distinct. Misty was also viewed in-world.

The NPC fixture injects a separate bound sprite pose into the private indoor
scene and moves it along a synthetic lane. This verifies the rendering path;
it is not a claim that all story scripts or NPC AI have been played through.

Evidence: npc-rig-hand-guard.log, npc-rig-hand-guard-gen2.log,
hero-regression-after-npc.log; npc-motion-gallery/main.lua and its four PNGs.
The runtime inventory has 198 non-action human atlases. Ten exact sources now
have reviewed arm profiles; 188 remain pending (human-arm-coverage.csv).
NPC eye expressions, smiles, folded arms and other acting poses remain open.
Brock's source eyes are already closed; no new blink overlay was added to him.
The broader goal remains incomplete.

## Common NPCs and palette sources

Fisher, Lass, Super Nerd and Gentleman now have source-reviewed hand profiles.
Their neutral/A/B production-mesh galleries were viewed in all directions.
Fisher's equipment-side arm and Gentleman's cane-side arm stay at zero swing
in front/back views; the free arm moves. Side views target the free foreground
arm. The new per-arm gains default to one for existing profiles. A geometric
check verifies pinned-hand vertices stay at neutral coordinates while the
other arm actually moves, alongside the full-versus-reduced surface test.
The dense test reference was extended with the same gain capability; the
pinned-coordinate check is an additional behavioral assertion.

Eight cataloged palette variants have exact alpha-byte equality with their
base atlas (dimensions and source hashes recorded in npc-variant-geometry.json).
Only those exact variant suffixes are admitted by these profiles. Native Gen2
checks exercise all eight variant bindings and all twelve cells each, including
protected-hand pixels and rejection of an unreviewed filename. Gen1's existing
binder intentionally rejects these palette files; its eight rejections are
verified rather than bypassed. The original six heroes remain covered by the
separate pixel/geometry regression.

Evidence: npc-common-rig.log, npc-common-variants-red.log,
npc-common-variants-crystal.log, heroes-after-common-npcs.log. Pixel comparisons
and native pose injection have the same limits described above. This does not
add eye expressions or other acting poses to these NPCs.

Current exact-source coverage is 22 of 198 non-action runtime-manifest entries
(14 base sources and 8 palettes); 176 remain pending. This inventory includes
sources the Gen1 binder deliberately rejects, so it is not a claim that every
entry is selectable in both generations. All existing binder restrictions stay
in force. human-arm-coverage.csv records those notes.

## Source-specific NPC blinking

Oak, Elm and Misty now have procedural eye profiles for front, left and right.
Brock keeps his originally closed eyes. These profiles only admit their exact
base atlas suffix and 495×900 dimensions; no palette or unrelated face inherits
the boxes. Kris and these NPCs share one compiled procedural shader, while
mutable output canvases and idle clocks remain per actor. No new PNG is added.

The source-rendered gallery checks nine faces at open/half/full closure, original
alpha, unchanged opaque pixels outside the eye regions, independent output
canvases, a single shader compilation and no rear output. Misty's right eye box
was corrected after visual inspection. The later lid-occlusion update removes vertical iris compression in
intermediate stages; see HUMAN_BLINK_OCCLUSION.md. Oak's brow-to-eye boundary also merits further polish at close zoom.

Native Red and Crystal runs bind each NPC separately through the normal binder,
inject a stationary NPC pose into the production frame list, trigger the existing
idle clock after admission, and observe visible eyelid output in three directions.
They then check movement, classic and dialog cancellation. The same runs verify
Red/Kris closure cancellation on Vanilla, classic, actual player movement, dialog,
script runner and input lock, plus option/map restoration. This is renderer-level
NPC coverage, not a complete story/AI playthrough or global performance clearance.
Evidence: npc-blink-{red,crystal}.log, npc-blink-gallery.log and
npc-gallery/npc-blink-gallery.png. Full human-motion regression also passes.
Arm/source coverage remains 22/198; blinking coverage is nine base characters.


## Trainer profiles: Beauty, Blackbelt, Biker, Bird Keeper

Four further base cards now use reviewed hand regions in all four directions.
Beauty's bag-side arm is pinned in front/back; Bird Keeper's glove-side arm is
pinned there. Side views retain a small foreground-arm swing. Biker needs a
second composition guard on the left-facing cell to remove a displaced glove
from the original walking image; that guard does not drive a second moving arm.
The four source-rendered walk galleries and weight-shift galleries were inspected.

Beauty and Blackbelt each have two exact palette aliases. Their dimensions and
alpha geometry match the base sources; native Gen2 tests additionally check all
12 composed cells of each alias. Gen1's existing rejection of those palette
files is asserted, not overridden. Native Red/Crystal tests bind all four base
NPCs independently and pass dense/reduced geometry comparisons, per-actor mesh
isolation, protected hand pixels, cache reuse, floor-anchored shift/restore, and
four-direction rendering. Evidence: npc-trainers-{red,crystal}.log. The first
Red test stopped at the unrelated map-reload assertion after four rendered
frames; the test now waits up to two seconds for the actual presentation state
change, and both final runs pass. No production event behavior was changed.

Exact-source arm coverage is now 30/198 (18 base sources, 12 palettes), with 168
pending. These four NPCs have breathing and weight shifting, but no new eye or
acting-pose profiles yet. Existing full-game/grid/performance limitations remain.


## Scientist, Rocket Grunt Male, Clerk, Cooltrainer Female

Four further base sources have manually reviewed four-direction hand profiles.
The Cooltrainer's two side views need second composition-only hand guards to
remove detached hand remnants from the old step texture. Walk/neutral/weight
shift galleries were rendered with production code and visually inspected.
The scientist's coat still follows the existing lower-body blend; its hem is
not a separately articulated garment.

Six exact Scientist/Clerk/Cooltrainer palette aliases preserve base alpha
geometry. Native Red/Crystal runs verify all four base sources, twelve composed
cells each, shared immutable geometry with independent actor meshes, anchored
weight shifts/restoration and the existing scene/option cancellation tests.
Crystal also checks all six palette sources; Red asserts their existing binder
rejection. Logs: npc-town-red.log and npc-town-crystal.log. The test NPC poses
are injected into the real rendering list; these are not complete AI/story tests.

Arm coverage is now 40/198 sources (22 bases and 18 palettes), with 158 pending.
No new face profiles or acting poses are added in this batch. Full-game animation,
grid/3D-cell coverage and final performance acceptance remain incomplete.


## Teacher, Pokefan Male, Sailor, Cooltrainer Male

Four base cards and eight exact palette sources are now source-reviewed.
Teacher keeps the book arm pinned in front/back; the left-facing side profile
uses only a composition guard and no moving foreground arm. A geometry assertion
confirms every side-view vertex remains unchanged by stride in this held pose.
The opposite free arm retains its normal small swing. Pokefan's backpack/Pichu
remain in the original upper-body image; no separate Pichu animation is added.

Production walk and weight-shift galleries were inspected in four directions.
Red/Crystal native runs pass independently bound NPC rendering, all twelve cell
comparisons, independent actor meshes, shared templates, pinned-arm checks and
anchored shift/restore. Crystal checks all eight palette sources; Red verifies
the existing palette rejections. Palette dimensions/alpha match their bases.
Evidence: npc-routes-{red,crystal}.log and npc-gallery images. These renderer
fixtures do not establish full-story behavior or a global performance guarantee.
Teacher's original side neutral cell already has an offset-foot stance; the
profile preserves that artwork, so a true authored standing replacement remains
an artistic follow-up rather than being implied by the geometry tests.

Exact-source arm coverage is 52/198 (26 bases, 26 palettes), leaving 146 pending.
No eye or additional acting-pose profiles are added in this batch. All previous
full-game/grid/performance limitations remain open.

## Gramps, Team Rocket Male, Gym Guide, Nurse

Four more base sources have source-specific hand regions in four directions.
Their production walk and weight-shift galleries were inspected; no detached
hand/glove remnants were observed in these sample poses. No separate service
acting or facial-expression assets are introduced. Existing modal/script guards
still govern idle admission; complete healing-service/story playback is pending.

Native Red/Crystal tests pass independent NPC records, reduced/dense geometry,
protected neutral hand regions, all twelve composed cells, shared templates,
per-actor meshes, and anchored shift/restore. Six Gramps/Gym Guide/Nurse palette
aliases preserve dimensions and alpha; Crystal checks their composed cells and
Red asserts its original palette rejection. Logs: npc-services-{red,crystal}.log.
The original Game/World is not edited by these presentation profiles.

Exact-source arm coverage is now 62/198 (30 bases, 32 palettes), with 136 pending.
The renderer-fixture evidence does not establish full-story compatibility,
complete grid/3D-cell support or acceptance of the remaining performance spikes.


## Hiker, Police Officer, Bug Catcher, Swimmer

Four base sources and six exact palettes are reviewed. Hiker's right-facing
composition has a second hand guard to remove an old step-hand fragment. Bug
Catcher pins the net arm in front/back and now also uses optional pinSecond
side-view geometry: the second hand region is excluded from the free-arm sway.
Independent coordinate assertions verify the prop region stays at its neutral
position while the free arm moves. Profiles without pinSecond retain the prior
weights. This permits a held object and a moving free arm in the same side view.
Production walk and idle-shift galleries were inspected for all four sources.

Red/Crystal native tests pass base NPC records, four directions, dense/reduced
geometry, protected hand pixels, cell cache/actor isolation and anchored shift.
Crystal accepts and checks all six palettes. Red rejects the four general
-variant- palettes but accepts/checks the two explicit bug-teal/bug-navy sources:
walking_sprites.lua already maps these to bug-catcher. No binder restriction was
changed. Evidence: npc-outdoors-{red,crystal}.log. These are renderer fixtures,
not a complete trainer AI/story or swimming-action playthrough.

Exact-source arm coverage is 72/198 (34 bases, 38 palettes), leaving 126 pending.
No new face or acting-pose profiles are added. Prior full-game/grid/performance
limitations remain open; source-reviewed does not mean final artistic perfection.

The side-prop change also passes all-six hero dense/reduced geometry comparisons
and byte-identical old/new cell composition, resource checks and the full human
regression. Logs: heroes-after-side-prop-pin.log and npc-outdoors-regression.log.

## Four leader profiles — 2026-09-11

Added exact Sabrina (Gen2), Lt. Surge, Blaine and Falkner sources. All four
passed native Red and Crystal flat/relief geometry and lazy-cell checks in
a private interior fixture. All four directions and three gait cells were
visually inspected. Side-hand guards remove rear-hand remnants for Blaine
and Falkner. Blaine alone uses a lower .80–.825 leg transition to preserve
his coat hem. The other profiles keep the original .685–.710 transition;
all-six hero native pixel/geometry regression and the full human runner pass.
This is source-level rig coverage, not a facial-expression or story-acting approval.
Coverage is now 76/198 exact atlases; 122 remain pending.

## Mother conversation baseline — 2026-09-11

Private Red test in REDS_HOUSE_1F, using the real mother NPC, world:interact
and the native text script: four approach directions in natural, classic
and Vanilla, 12 passing cases. Verified speaker-facing, dialog acquisition,
ambient-motion suppression and NPC release after closing the text.
Screenshots confirm the HD mother in natural mode. Player positions were
placed by the fixture; this does not test navigation or chair collisions.
Current behavior turns the entire sprite. Separate head yaw, sitting and
conversation expressions are NOT implemented or approved by this test.
The desired follow-up is a mother-only, target-specific conversation pose;
the general modal guard must remain intact.
Evidence: mother-talk-red.log and integrated-mother-talk.lua in private QA.

## Mother arm profiles — 2026-09-11

Exact reds-mother v2 and johto-mother v1 atlases now have reviewed hand
landmarks and guarded side-hand regions. Red's mother's leg transition is
.84–.865; Johto mother's is .87–.895, keeping the skirt/apron hem neutral.
The source upper-body pixels stay intact. Both galleries were inspected
in four directions and neutral/A/B poses. Both profiles passed independent
NPC binding, mesh reduction and lazy-cell/source-pixel checks in native
Red and Crystal private interior fixtures. Full human regression passed.
Coverage: 78/198 exact sources, 120 pending.

These profiles add arm gait and weight-shift admission, not new eyelid
art or separate head yaw. Red mother's ponytail overlaps the shoulder in
front/back views; Johto mother's hair overlaps the neckline. A horizontal
head cut cannot preserve these silhouettes. Conversation head work still
needs a complete head/hair layer and the body pixels it uncovers, plus a
target-specific conversation hook that leaves the general modal guard intact.
Evidence: npc-mothers-{red,crystal,regression}.log, npc-mothers-reviewed.json
and npc-motion-{reds-mother,johto-mother}.png in the private QA directory.

## Mother head-turn art exploration — 2026-09-11

Built-in Imagegen produced six front-torso listening poses with forward,
25-degree and 50-degree head views in both directions. Real alpha was
verified after dedicated background extraction, and the result was drawn
in a separate native LOVE gallery. QA-only artifact: art-review/
mother-head-turn-v1/{poses.png,gallery.png,inspection.json,PROMPTS.md}.

Not approved for game inclusion: this is not separated head/body art; body
pixels vary despite foot registration, and appearance differs from the
original atlas. The 19-file installation payload and 78/198 rig coverage
are unchanged. Next integration must hold body geometry/art stable and
validate neckline/hair joins, rather than directly swapping these cells.

## Seated mother and separate head — native prototype

Private Red fixture now places a generated seated mother on the existing
chair, compensating native +5 elevation with -5 Y and using +8 Z / height18.
Both real left/right conversations render an independently turning head.
Native NPC positions stay unchanged. Actual renderer-option getter tests
pass for Classic, Vanilla and restoration. This is QA-driver integration
only; no claim of production release or Gen2 seat support.

The shared standing body plate passed 627,200 exact RGBA comparisons
below the head/hair boundary. Head layers use per-source tuft matting;
optical flow is analyzed offline into bounded vector fields, not computed
on gameplay frames. Seated composition uses a fixed body canvas.
Evidence: mother-head-layers.log, mother-seat-native-final.log, and
art-review/mother-seated-v1. Interactive driver: integrated-mother-seat.lua
with MOTHER_SEAT_DEMO=1; no Orange environment is involved.


## Daisy and distinct NPC Blue arm sources — 2026-09-11

Added exact daisy-oak v2 and NPC blue v2 hand profiles. HumanRig.profileFor
resolves a role's explicit alternate artwork using complete path-suffix
segments, so NPC Blue receives his own hand landmarks while hero Blue keeps
his original profile. Gym Blue, bicycle Blue, unknown revisions, misleading
suffix collisions and ambiguous declarations are excluded in unit checks.

Daisy uses a .72–.75 lower-body blend to connect her skirt to the moving legs;
a later blend left a visible gap and was rejected in gallery review. NPC Blue
uses wider composition-only hand masks in side views to remove moving-hand
remnants, while mesh deformation retains the narrower reviewed hand radii.
All other profiles keep the old default mask radius. Both final four-direction,
neutral/A/B galleries were inspected. No source PNGs were changed.

Native Red and Crystal private interior fixtures independently bind both NPC
sources, exercise all directions, compare dense/reduced flat/relief surfaces,
check protected neutral hand pixels, lazy cells and cache reuse. These are
renderer/source checks, not completion of Daisy's sitting/dialogue story poses.
The historical geometry verifier now supplies the residency manager's local
bookkeeping stubs when extracting only the mathematical deformation function;
resource limits have separate unit and real-GPU stress coverage.

Evidence: npc-opening-red.log, npc-opening-crystal.log,
human-motion-opening-profiles.log, opening-rig-boxes.json and
npc-opening-{daisy-oak,blue}.png. The exact-source enumeration includes explicit
alternates: 80/198 approved arm sources, 118 pending. Facial/acting coverage
and full performance acceptance remain separate unfinished requirements.


## Bill and Kurt arm sources — 2026-09-11

Added exact bill and kurt v1 source profiles with source-reviewed neutral hand
boxes and protected side hands. Bill uses the .685–.710 leg transition; the
initial .76–.785 interval left a visible side-view pelvis gap and was rejected.
Kurt retains .76–.785 to preserve his short jacket. Both final neutral/A/B
galleries were inspected in four directions. Source PNGs are unchanged.

Private Red and Crystal interior render fixtures pass independent NPC binding,
all four directions, dense/reduced flat/relief geometry equivalence, protected
neutral hand pixels and demand-only cell/cache checks for both sources. The
full human-motion runner passes, including 1,080 exact expanded-triangle pose
comparisons and Classic/Vanilla/missing-source render roundtrips. Source
registration is now 82/198, with 116 still pending. These additions do not
provide Bill/Kurt eyelids, seated poses or story-specific head turns.

Evidence in private non-Orange QA: npc-hand-probe-bill-kurt.json,
npc-bill-kurt-reviewed.json, npc-bill-kurt-gallery.log,
npc-bill-kurt-red-render.log, npc-bill-kurt-crystal.log,
npc-bill-kurt-regression.log and shots/npc-motion-{bill,kurt}.png.

An initial run of the older integrated-npc-rig.lua stopped before testing either
NPC at its map.reloaded state-identity assertion. The old state table appeared
unchanged after the synthetic event (npc-bill-kurt-red.log). This is an open
context diagnostic, not a successful map-reload test. The subsequent focused
renderer driver does not cover that assertion; its passing results must not
be used to dismiss it.


## Map-reload diagnostic resolved: wait for actor rendering — 2026-09-11

The prior synthetic map.reloaded failure was a fixed-frame QA synchronization
error, not evidence that production reused a stale animation on its next draw.
A native probe verifies the live renderer epoch advances immediately (29 to
30), the same current imageDefs table is inspected, and the actor receives a
new motion table on its subsequent draw (frame 3 in this probe). Several
completed love.draw calls can occur before that actor is drawn again.

The canonical private integrated-npc-rig.lua now asserts immediate renderer
epoch invalidation, then waits for the actor's matching epoch with a five-second
wall-clock deadline, and only then asserts a new non-nil motion table. It does
not weaken the identity assertion or accept an absent actor draw. Full native
Red and Crystal runs pass this condition plus real dialogue, script runner,
input lock, options roundtrip, movement/blink cancellation, Classic/Vanilla and
Bill/Kurt geometry/pixel checks. Production code did not need a reload change.

Evidence: map-reload-probe-red.log, integrated-map-reload-probe.lua,
map-reload-draw-synchronized-{red,crystal}.log and integrated-npc-rig.lua in
private non-Orange QA. This resolves the specific synthetic reload diagnostic;
it does not resolve head-morph ghost contours or universal animation coverage.


## Johto ace trainers: reviewed arm profiles (2026-09-11)

Added exact v1 profiles for ace-trainer-male-gen2 and ace-trainer-female-gen2.
Neutral hand boxes were inspected in all four atlas rows. The female profile
preserves the long hair and shorts in the reviewed neutral/A/B gallery.
The initial male side profile left a detached source-hand remnant in stride B;
a second source-hand protection region at normalized x=.82 (left-facing) and
.18 (right-facing) removes it. The corrected twelve-pose gallery was inspected.
No sprite PNG was modified. Atlas coverage is now 84/198, leaving 114 pending.

Native isolated NPC render fixtures passed both profiles in Red and Crystal:
independent NPC records, four directions, pixel composition and shared mesh
geometry/lifetime checks. The same driver also passed its existing player
Classic/Vanilla, dialogue, script/input lock and map/option epoch checks.
These context checks concern the driver player, not a claim that each new
NPC was separately exercised in every story scene. Logs contain no runtime
errors, and the full human regression runner passes (84 admitted sources).
The Red fixture explicitly binds these Gen2 cards for renderer compatibility;
it does not change Gen1 map bindings. The inventory lists 14 male and 17
female references; live encounters at those references remain to be reviewed.

Evidence: ace-trainer-profiles/ in the review package contains source landmark
notes, galleries, native logs, driver and regression output. No new blink
profiles were added here. General head-turn ghosting, all remaining NPCs,
additional acting poses and wider performance acceptance remain open.


## Johto ace-trainer idle blinking (2026-09-11)

Both exact v1 ace-trainer sources now have procedural front/left/right eye
profiles. Source inspection supplied the hair-adjacent front eye missed by
the automatic probe: male [65,68,78,83], female [63,59,76,76]. Remaining boxes
were checked against the source and the open/half/closed renderer gallery.
The gallery verifies unchanged source alpha and opaque pixels outside eye
regions, shared shader allocation, and no rear output. No raster edits or
additional eyelid assets were needed. Ordinary exact blink coverage is now
17 sources; arm coverage remains 84/198.

Native isolated NPC fixtures pass Red and Crystal scheduled idle cycles,
side output, rear exclusion, moving-eye cancellation, Classic and Vanilla.
Observed closure peaks: male/female .99857/.99965 in Red and .98596/.99020 in
Crystal. Logs contain no runtime errors. The native Red female closure capture
was visually inspected. The full human regression runner passes. This is
renderer/fixture evidence, not a claim of checking all real trainer encounters
or their dialogue scenes. Those wider scenes and remaining NPC sources stay
open. Evidence: ace-trainer-blink/ in the review package. No Orange tests.


## Approved Gen1 youngster family (2026-09-11)

Added an exact arm profile for the bald Gen1 youngster and its approved green
and red palettes. All three alpha channels are byte-identical. Neutral hands
were inspected and front/back/side windows fitted; side source-hand protection
also suppresses distant remnants from the walking atlas. Twelve-pose galleries
for base/green/red were inspected. No sprite PNG changed. Arm source coverage
is now 87/198, leaving 111 sources without reviewed profiles.

The initial side configuration carried an inappropriate equipment gains field.
Side motion does not consume that field, but the equipment invariant checker
rightly rejected the advertised pinning. It was removed; the figure carries no
prop, and actual side geometry was unchanged. An initial test also incorrectly
assumed the legacy cap atlas was globally rejected. Source inspection confirms
WalkingSprites applies that rejection only to Gen1. The corrected test checks
the existing Gen1 rejection; Gen2 binding policy remains unchanged. No profile
for the cap artwork is added by this change.

Corrected isolated native NPC fixtures pass Red and Crystal: four directions,
shared geometry, source pixel composition, independent record and both palettes.
Their pre-existing player context checks also pass Classic/Vanilla, dialogue,
script/input lock and map/option invalidation. These context checks are not a
claim that each youngster encounter was individually tested. The base inventory
has 34 references; live story/route acceptance remains open. Logs contain no
runtime errors. No new youngster blinking profile is included in this step.
Evidence: youngster-profiles/ in the review bundle. No Orange interaction.


## Approved youngster family blinking (2026-09-11)

Added exact procedural front/left/right profiles for the bald youngster and
both approved palettes, with distinct alternate profile identities. Manually
reviewed eyes are front [59,52,74,68] and [89,52,104,68], left [57,282,67,299],
right [97,739,107,756]. The automatic face probe also found eyebrow/scalp/ear
components, which were excluded. No source PNG or generic binding policy changed.
Ordinary blink coverage is now 20 exact atlas sources; arm coverage stays 87.

Base/green/red GPU galleries pass original alpha, unchanged opaque pixels
outside eye bands, shared procedural shader and rear exclusion. Base and green
galleries were visually inspected. Native isolated NPC tests exercise each
palette in Red and Crystal with scheduled closure, both sides, rear exclusion,
movement cancellation and live Classic/Vanilla. Closure peaks are .99746/1/1
in Red and 1/1/1 in Crystal. All logs lack runtime errors. The full regression
runner passes. Each native palette case creates its own NPC fixture; it does
not prove a same-live-NPC palette swap or every real story encounter.

Small bright edge remnants visible around the original bald head are present
in the source and deliberately unchanged by the eye-only render. They remain
an artwork/edge-cleanup concern, not a claim of final all-character perfection.
Evidence: youngster-blink/ in the review package. No Orange interaction.


## Youngster bright-rim diagnosis and shader prototype (2026-09-11)

Read-only source inspection finds an almost-white horizontal run at source
row 12, x75..88, directly above the dark scalp outline. Most of that run has
alpha 241..246/255, so increasing a low-alpha cutoff would not remove it.
Eight-connected alpha components do not isolate it from the main body;
a generic small-island removal would miss this artifact. The rim is already
in the source; it is not a blink or double-render product.

A private LOVE shader prototype removes pale near-neutral boundary pixels
only where transparent exterior and a dark opaque inward neighbour establish
a rim. The first broad pass removed 432 pixels but also touched shoes, so it
was not selected. The revised version limits treatment to the first 105 rows
of each 225-row cell and checks up to two pixels across the boundary. GPU
checks assert that only alpha-clearing occurs, every changed pixel is within
two pixels of transparent exterior, clothing/shoe rows remain unchanged, and
retained opaque pixels preserve RGB. All twelve source poses were rendered
side by side and inspected. Large scalp/side ticks are reduced; tiny edge
remnants remain, so this is not complete cleanup acceptance.

This is a shader/code prototype; source PNGs are unchanged. It is not wired
into production. Integration must keep the correction consistent between
rig composition, blink output and grid preparation, and preserve exact
Classic/Vanilla behavior. It must not clean only the base rig and let old
bright pixels reappear with an eyelid canvas. Other character sources need
separate review before any general admission rule. Native/performance and
cross-palette acceptance are still pending. Evidence: youngster-rim-lab/,
youngster-rim-prototype.log, youngster-head-rim-prototype.log and comparison
screenshots in private QA. No Orange interaction.


## Shared youngster rim integration (2026-09-11)

The earlier QA-only prototype is now integrated through optional human_rim.lua.
Only the three reviewed 495x900 bald youngster sources opt in, via matching
rig and blink profiles. Both shaders share the same source sampling rule;
grid preparation consumes the resulting animated texture. Source PNGs remain
unchanged. Other sources do not opt in. Classic/Vanilla retain their existing
render path. This supersedes the prototype-only status above, not its visual
limitations: tiny edge remnants and other characters still need review.

The separate graphics-enabled tests/human_rim_runner passes against actual
LOVE shaders: 280 alpha removals across three palettes and three neutral
views, rig/blink alpha agreement, unchanged clothing and opaque non-eye RGB,
strict profile/dimension admission, and rear/moving blink exclusion. This is
a different sampling scope from the earlier all-twelve-cell prototype count.
The ordinary non-graphics human_motion_runner also passes.

Native isolated NPC fixtures in Red and Crystal pass all three palettes with
grid off and on: scheduled closure, side output, rear exclusion, moving blink
cancellation, Classic and Vanilla overlay shutdown. Grid runs assert that
the exact target NPC entered animated grid preparation without grid errors.
All four logs are checked for runtime errors as well as per-palette markers.
Grid/base/Gen1 and flat/red/Gen2 captures were visually inspected. They show
closed eyelids and reduced scalp ticks; still images cannot establish absence
of every transient flash. Fixtures do not prove real story encounters or a
live same-NPC palette change. No host-isolated performance approval is claimed.
Evidence: youngster-rim-integration/ in the review package. No Orange access.

### Kai/Bugsy — 100 Armquellen, 30 Blinkquellen

Exakte v1-Handenden ohne Knieverformung, dezente Gewichtsverlagerung, Beine mit weichem Übergang und abwechselndem Vorder-/Rückenschritt ergänzt. Augen vorn und seitlich blinzeln über den unveränderten Shader. GPU-Galerien und native Gen2-Blinkansicht visuell geprüft; Gen1/Gen2 mit eigenen Arm- und Blinkläufen samt Classic-/HD-aus-/Natürlich-Rückkehr bestanden. Voller Runner bestanden. Keine Rasteränderung, kein Orange. Seitliche Quellkadenz noch offen, keine Arena-Storyprüfung behauptet. Details: bugsy-rig/README.md im Prüfpaket.

### Kai: echtes Arenaobjekt und Dialoggrenze

AZALEA_GYM_obj_1 erhält bei exakt eigenem Script-/Textfenster ruhiges Atmen und Blinzeln im bestehenden Acting-Piloten. Nativer Arena-Dialog besteht mit 2088 Bildern ohne Zustandsverlust, Classic-/HD-Rückkehr und ohne Belebung unbeteiligter Figuren. Echter Kampfbeginn sperrt Dialog-Idle in30 Bildern. Dieser Kampflauf fällt wegen einer bereits in19039d6c fehlenden BattleBillboard.orientation-Funktion auf den nativen Hintergrund zurück; keine 3D-Kampffreigabe daraus ableiten. Voller Runner samt neuer Kampf-/Fremdtext-/Objektslot-Tests bestanden. Details: bugsy-scene/README.md. Keine neue Kopfpose oder Rasteränderung, kein Orange.

### Jens/Morty und Bianka/Whitney — 102 Arm-/32 Blinkquellen

Exakte v1-Handregionen, dezente Gewichtsverlagerung, geprüfte Körperübergänge und abwechselnde Vorder-/Rückenschritte ergänzt. Idle-Augen vorn/seitlich über unveränderten Shader. Automatischer Morty-Augenprobe verwechselt Blondhaar mit Gesicht und wurde manuell korrigiert. GPU-Galerien und native Gen2-Ansichten visuell geprüft; vier native Gen1-/Gen2-Arm-/Blinkläufe samt per-Rolle-Classic-/HD-Rückkehr und voller Runner bestanden. Seitenkadenz und Story-/Dialogfreigaben weiterhin offen. Keine Rasteränderung, kein Orange. Details: morty-whitney/README.md im Prüfpaket.
